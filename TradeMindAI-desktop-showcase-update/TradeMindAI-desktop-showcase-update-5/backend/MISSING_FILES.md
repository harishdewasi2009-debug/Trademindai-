# Status: no missing files

All files this backend `require()`s are present in this repo — the old
version of this note (from an earlier upload batch) is no longer accurate
and has been removed. The server fails to boot only if you haven't created
a `.env` file yet; see README.md → "Environment variables".
